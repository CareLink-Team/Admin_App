import 'package:flutter/material.dart';

class CaretakersList extends StatelessWidget {
  const CaretakersList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Caretakers List'));
  }
}
